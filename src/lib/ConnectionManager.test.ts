import { ConnectionManager } from './ConnectionManager'

describe('Connection Manager', () => {
  it('can be constructed', () => {
    const manager = new ConnectionManager()
    expect(manager).toBeTruthy()
  })

  it('can connection to another manager', () => {

  })
})
