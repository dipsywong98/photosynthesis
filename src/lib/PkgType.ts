export enum PkgType {
  ALERT = 'alert',
  XD = 'xd',
  JOIN = 0,
  MEMBER_CHANGE,
  NEW_JOIN,
  ACK,
  PLAYER_LEFT,
  CHANGE_HOST,
  RENAME,
  START_GAME,
  GAME_EVENT,
  UPDATE_GAME_STATE,
  DISPATCH,
}
